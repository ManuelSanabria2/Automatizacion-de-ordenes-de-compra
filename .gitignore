# ── Secretos ─────────────────────────────────────────────
# Los .env y .env.local NUNCA se suben (en ningún nivel del repo)
.env
**/.env
.env.local
**/.env.local
.env*.local
!.env.example
!**/.env.example

# ── Python (backend) ─────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# ── Next.js / Node (app) ─────────────────────────────────
# (app/.gitignore generado por create-next-app también aplica)
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
npm-debug.log*

# ── Datos reales de la empresa (solo locales, nunca al repo) ─
Cotizaciones de prueba/
Requisición Abastecimientos excel.xlsx
# Set de evaluación del matching: sale de alias_productos reales (NIT del
# proveedor, nombres del catálogo, UUIDs). Se regenera — ver evaluar_resolucion.py
backend/tests/datos/

# ── Herramientas de análisis (grafo de conocimiento local) ─
graphify-out/

# ── IDEs y sistema ───────────────────────────────────────
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
desktop.ini
